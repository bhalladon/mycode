    # item = ['bac', 'xyz', 'tyr']
    # total_item_len = len(item)
    # print total_item_le1
    #
    # for x in range(total_item_len):
    #     print x

    # a = [6, 7, 8, 9, 10]
    # b = []
    # for x in a:
    #     if a.index(x) >= 2:
    #         c = b.append(x)
    # for x in a:
    #     if a.index(x) < 2:
    #         c = b.append(x)
    #
    # print b

# var = raw_input(str("Enter a string to reverse:"))
# string_length = len(var)
# print "Total lenght of the stringis: " + str(string_length)
# rev_string = []
# while string_length > 0:
# # for x in range(string_length):
# #     if x < string_length:
#     rev_string += var[string_length -1]
#     string_length = string_length -1
# print rev_string

# """slicing"""
# var = "chakde"
# for x in range(len(var)):
#     print var[x:2]